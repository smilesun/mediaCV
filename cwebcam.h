#ifndef CWEBCAM_H
#define CWEBCAM_H

#include <opencv2/core/core.hpp>
#include <opencv2/highgui/highgui.hpp>
#include <opencv2/imgproc/imgproc.hpp>
#include <QImage>
#include <QTimer>
#include <QtGui>
#include <QMessageBox>
#include <QWidget>
#include "mainwindow.h"


/*
class CWebCam
{
public:
    CWebCam(Ui::MainWindow * parent);
    //void store();//store video on disk
    void exec();
    //void nextFrame();
    //void writeNextFrame();
    //void paintEvent(QPaintEvent *e);
    Ui::MainWindow *pwin;
    bool isCamera;
    CvCapture *capture;
    CvVideoWriter* writer;
    IplImage *iplImg;
    IplImage *frame;
    cv::Mat* Matframe;
    QImage *qImg;
    QTimer *timer;
};

*/
#endif // CWEBCAM_H
